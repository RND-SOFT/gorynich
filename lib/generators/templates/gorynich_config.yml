development:
  default:
    db_config:
      adapter: postgresql
      encoding: unicode
      host: localhost
      pool: 5
      port: 5432
      database: gorynich_default
      username: postgres
      password: password
  local:
    db_config:
      adapter: postgresql
      encoding: unicode
      host: localhost
      pool: 5
      port: 5432
      database: gorynich_local
      username: postgres
      password: password
    secrets:
      uris:
        - http://localhost:3000

test:
  default:
    db_config:
      adapter: postgresql
      encoding: unicode
      host: localhost
      pool: 5
      port: 5432
      database: gorynich_default_test
      username: postgres
      password: password
  local:
    db_config:
      adapter: postgresql
      encoding: unicode
      host: localhost
      pool: 5
      port: 5432
      database: gorynich_local_test
      username: postgres
      password: password
    secrets:
      uris:
        - http://localhost:3000
